import { PageStub } from "@/shared/ui/page-stub"

export default function ContaPage() {
  return (
    <PageStub
      title="Conta"
      description="Perfil do usuário + camada de assinatura (gate +18). Autenticação e pagamento entram numa leva futura."
      feature="visão §8"
    />
  )
}
