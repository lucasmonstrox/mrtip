import { PageStub } from "@/shared/ui/page-stub"

export default function HistoricoPage() {
  return (
    <PageStub
      title="Histórico · CLV"
      description="A prova de honestidade: CLV (closing line value), yield, acerto com n, plot de calibração e ledger imutável append-only (hash + timestamp do pick)."
      feature="SIN-012 · MOD-001"
    />
  )
}
