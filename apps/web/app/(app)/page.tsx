import { RoundHubPage } from "@/features/round-hub"

// Primeira página logada — Hub da Rodada. ⚠️ Protótipo com dados MOCK (odds/EV/prob fictícios):
// o coração de valor depende de SIN-012 (odds) + MOD-001 (motor) + CORE-001, ainda não ingeridos.
export default function HubRodadaPage() {
  return <RoundHubPage />
}
