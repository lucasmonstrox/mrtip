import { PageStub } from "@/shared/ui/page-stub"

export default function DossiePage() {
  return (
    <PageStub
      title="Dossiê do Jogo"
      description="O 'porquê' completo de um pick: pickbox (veredito + EV + prob calibrada + odd), dimensões com peso, mercado & odds, fontes auditáveis e chat. Índice → /jogo/[matchId]."
      feature="DOS-001 · MOD-* · AGT-001"
    />
  )
}
