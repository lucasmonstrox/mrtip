// Barrel agregador do schema (um arquivo por domínio sob ./schemas). Mantém os imports
// `../db/schema` (client, services) e o `drizzle.config.ts` apontando para cá funcionando.
export * from "./schemas"
