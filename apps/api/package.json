{
  "name": "@workspace/api",
  "version": "0.0.1",
  "type": "module",
  "private": true,
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "dev": "bun run --watch src/index.ts",
    "start": "bun run src/index.ts",
    "build": "bun build ./src/index.ts --target bun --outdir ./dist",
    "deploy": "wrangler deploy",
    "cf-dev": "wrangler dev",
    "lint": "eslint",
    "format": "prettier --write \"**/*.ts\"",
    "typecheck": "tsc --noEmit",
    "db:up": "docker compose up -d --wait",
    "db:down": "docker compose down",
    "db:generate": "drizzle-kit generate",
    "db:migrate": "bun run src/db/migrate.ts",
    "db:seed": "bun run src/db/seed.ts",
    "db:lineups": "bun run src/db/seed-lineups.ts",
    "db:injuries": "bun run src/db/seed-injuries.ts",
    "db:gols": "bun run src/db/seed-gols.ts",
    "db:coaches": "bun run src/db/seed-coaches.ts",
    "db:reset": "docker compose down -v && docker compose up -d --wait && bun run src/db/migrate.ts && bun run src/db/seed.ts"
  },
  "devDependencies": {
    "@types/bun": "^1.3.14",
    "@workspace/eslint-config": "workspace:*",
    "@workspace/typescript-config": "workspace:*",
    "drizzle-kit": "^0.31.10",
    "eslint": "^9",
    "typescript": "^5"
  },
  "dependencies": {
    "@elysiajs/cors": "^1.4.2",
    "@elysiajs/openapi": "^1.4.15",
    "drizzle-orm": "^0.45.2",
    "elysia": "^1.4.29",
    "http-status-codes": "^2.3.0"
  }
}
